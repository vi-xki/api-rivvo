# Copy to .env and update values for your local environment
# MySQL connection string
DATABASE_URL="mysql://db_user:db_password@localhost:3306/rivvo_local"

# JWT secret used by the backend (change in production)
JWT_SECRET="changeme"

# Optional: port to run the server
PORT=3000
